<%= simple_form_for [chat], html: { class: "form chat" }, data: { turbo_frame: "#{nested_dom_id(@chat, "messages")}"} do |f| %>
  <%= form_error_notification(chat) %>
  <%= f.submit class: "btn btn--secondary" %>
<% end %>

