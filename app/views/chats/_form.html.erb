<%= simple_form_for [user, chat], html: { class: "form chat" } do |f| %>
  <%= form_error_notification(chat) %>

  <%= link_to "Cancel", user_chats_path(@user), class: "btn btn--light" %>
  <%= f.submit class: "btn btn--secondary" %>
<% end %>

